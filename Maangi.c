// program that accepts two integers from the user and the products of the two integers
# include <stdio.h>
int main ()
{
int s,t,p;
printf("\n enter the first number:");
scanf("%d",&s);
printf("\n enter the second number:");
scanf("%d",&t);
p=s*t;
printf("the product is %d\n",p);
}